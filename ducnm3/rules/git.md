# Git Rules

- Keep all changes inside `ducnm3/` unless the user explicitly authorizes another path.
- Work directly on `ducnm3` unless the user explicitly requests a feature branch.
- Use `feature/ducnm3_<short-description>` for requested feature branches.
- Keep each change focused; do not include unrelated cleanup.
- Do not commit, push, or open a pull request unless the user explicitly asks.
